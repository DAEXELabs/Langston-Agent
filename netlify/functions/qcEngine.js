const REQUIRED_FORMS = ["Customer Water Readings V2", "Plumbing Inspection V2"];
const EQUIPMENT_TERMS = ["Water Softener", "Reverse Osmosis", "ONE Filter", "Infinity Filter", "Carbon Filter", "Sediment Filter", "UV System", "PRV", "Water Heater"];
function norm(value){return String(value ?? "").trim().toLowerCase();}
function hasAny(text, keywords){const lower=String(text||"").toLowerCase();return keywords.some(k=>lower.includes(k));}
function safeGet(row, columns){for(const col of columns){if(row && row[col]!==undefined && row[col]!==null && String(row[col]).trim()!=="") return row[col];}return "";}
function rowText(row, columns){return columns.map(col=>row?.[col] ?? "").join(" ").trim();}
function normalizeFormStatus(status){const value=norm(status);if(["done","complete","completed","yes","true","1"].includes(value))return "Completed";if(["started","incomplete","partial","in progress"].includes(value))return "Incomplete";return "NO";}
function parseRequiredForms(value){if(!value)return{};if(typeof value==="object")return value;try{const parsed=JSON.parse(String(value));return typeof parsed==="object" && parsed!==null ? parsed : {};}catch{return{};}}
function getRequiredFormStatus(row, formName){const possible=[formName,formName.toLowerCase().replaceAll(" ","_"),formName.toLowerCase().replaceAll(" ","_").replaceAll("/","_")];for(const col of possible){if(row[col]!==undefined && String(row[col]??"").trim())return normalizeFormStatus(row[col]);}const forms=parseRequiredForms(row.required_forms);if(forms[formName]!==undefined)return normalizeFormStatus(forms[formName]);return "NO";}
function isCleanRow(row){const issueText=rowText(row,["issues","recommendations","coaching_focus","opportunity_summary","qc_status","review_status"]);const value=norm(issueText);const clean=new Set(["","clean","pass","passed","no issues","no issues found","no recommendations","no recommendations generated"]);if(clean.has(value))return true;if(hasAny(value,["no issues found","clean job","passed qc"]))return true;return false;}
function filterByDate(records, selectedDate){if(!selectedDate)return records;return records.filter(row=>{const value=row.report_date||row.created_at||row.completed_on||row.date||row.completedDate||row.completedOn;if(!value)return true;return String(value).slice(0,10)===selectedDate;});}
function analyzeQcData(records=[], selectedDate=null){const data=filterByDate(Array.isArray(records)?records:[], selectedDate);if(!data.length)return{jobsReviewed:0,flaggedJobs:0,cleanJobs:0,cleanRate:0,mainCoachingPattern:"Not found in available data.",requiredFormIssues:"Not found in available data.",fieldproSalesproUsageIssues:"Not found in available data.",paymentItemsNeedingReview:"Not found in available data.",followUpItems:"Not found in available data.",estimateRevenueOpportunities:"Not found in available data.",recommendedLeadershipAction:"Upload QC data or pull ServiceTitan data first."};const jobsReviewed=data.length;const cleanJobs=data.filter(isCleanRow).length;const flaggedJobs=jobsReviewed-cleanJobs;const cleanRate=jobsReviewed?Number(((cleanJobs/jobsReviewed)*100).toFixed(1)):0;const patternCounter={};const equipmentCounter={};const requiredIssues=[];let fieldproIssues=0,paymentReviewCount=0,followUpCount=0,estimateCount=0;const bump=k=>{patternCounter[k]=(patternCounter[k]||0)+1};for(const row of data){const combined=rowText(row,["issues","recommendations","coaching_focus","opportunity_summary","notes","tech_notes","summary"]);for(const form of REQUIRED_FORMS){const status=getRequiredFormStatus(row,form);if(status!=="Completed"){requiredIssues.push(`${form}: ${status}`);bump("Required forms / documentation");}}const fieldproValue=norm(safeGet(row,["fieldpro_utilized","fieldpro_used"]));const salesproValue=norm(safeGet(row,["salespro_notes_found","salespro_used"]));if(["false","no","0"].includes(fieldproValue)||["false","no","0"].includes(salesproValue)||hasAny(combined,["fieldpro was not utilized","no sales pro","no fieldpro"])){fieldproIssues++;bump("FieldPro / Sales Pro usage");}const paidValue=norm(safeGet(row,["paid","payment_status","paymentStatus"]));if(["unknown","","not found","not available","manual review","review"].includes(paidValue)||hasAny(combined,["payment status needs manual review","payment manual review"])){paymentReviewCount++;bump("Payment review");}const followValue=norm(safeGet(row,["follow_up_scheduled","follow_up_needed","followUpScheduled"]));if(["true","yes","needed","needs follow-up","needs follow up"].includes(followValue)||hasAny(combined,["follow-up may be needed","follow up may be needed","follow-up needed","follow up needed"])){followUpCount++;bump("Follow-up ownership");}const estimateValue=norm(safeGet(row,["estimate_created","estimate_status","estimateCreated"]));if(["true","yes","created"].includes(estimateValue)||hasAny(combined,["estimate","opportunity","recommended equipment"])){estimateCount++;bump("Estimate / revenue opportunity");}const equipmentText=`${safeGet(row,["recommended_equipment","recommendedEquipment"])} ${combined}`;for(const item of EQUIPMENT_TERMS){if(equipmentText.toLowerCase().includes(item.toLowerCase()))equipmentCounter[item]=(equipmentCounter[item]||0)+1;}if(hasAny(combined,["photo","picture","attachment"]))bump("Photos / attachments");}const mainCoachingPattern=Object.entries(patternCounter).sort((a,b)=>b[1]-a[1])[0]?.[0]||"Not found in available data.";const requiredCounts=requiredIssues.reduce((acc,item)=>{acc[item]=(acc[item]||0)+1;return acc;},{});const mostCommon=Object.entries(requiredCounts).sort((a,b)=>b[1]-a[1]).slice(0,4).map(([item])=>item);const requiredFormIssues=requiredIssues.length?`${requiredIssues.length} required form issue(s) found. Most common: ${mostCommon.join('; ')}`:"No required form issues found in available data.";const fieldproSalesproUsageIssues=fieldproIssues?`${fieldproIssues} job(s) show missing or unclear FieldPro/Sales Pro usage.`:"No FieldPro/Sales Pro usage issues found in available data.";const paymentItemsNeedingReview=paymentReviewCount?`${paymentReviewCount} payment item(s) need manual review.`:"No payment review items found in available data.";const followUpItems=followUpCount?`${followUpCount} follow-up item(s) need review or ownership.`:"No follow-up items found in available data.";const equipmentList=Object.entries(equipmentCounter).sort((a,b)=>b[1]-a[1]).slice(0,5).map(([item])=>item).join(', ');let estimateRevenueOpportunities=estimateCount?`${estimateCount} estimate/revenue opportunity item(s) found.`:"No estimate/revenue opportunities found in available data.";if(estimateCount&&equipmentList)estimateRevenueOpportunities+=` Recommended equipment mentioned: ${equipmentList}.`;const actions=[];if(flaggedJobs)actions.push("Review flagged jobs and validate important findings in ServiceTitan.");if(requiredIssues.length)actions.push("Coach required form completion before job closeout.");if(fieldproIssues)actions.push("Reinforce FieldPro/Sales Pro note usage.");if(paymentReviewCount)actions.push("Assign payment manual review items.");if(followUpCount)actions.push("Assign ownership for follow-up items.");if(estimateCount)actions.push("Review estimate opportunities for revenue follow-through.");return{jobsReviewed,flaggedJobs,cleanJobs,cleanRate,mainCoachingPattern,requiredFormIssues,fieldproSalesproUsageIssues,paymentItemsNeedingReview,followUpItems,estimateRevenueOpportunities,recommendedLeadershipAction:actions.length?actions.join(' '):"No major leadership action found in available data."};}
function formatDailySummary(s){return `Jobs Reviewed: ${s.jobsReviewed||0}\nFlagged Jobs: ${s.flaggedJobs||0}\nClean Jobs: ${s.cleanJobs||0}\n\nMain Coaching Pattern:\n${s.mainCoachingPattern||"Not found in available data."}\n\nRequired Form Issues:\n${s.requiredFormIssues||"Not found in available data."}\n\nFieldPro/Sales Pro Usage Issues:\n${s.fieldproSalesproUsageIssues||"Not found in available data."}\n\nPayment Items Needing Review:\n${s.paymentItemsNeedingReview||"Not found in available data."}\n\nFollow-Up Items:\n${s.followUpItems||"Not found in available data."}\n\nEstimate / Revenue Opportunities:\n${s.estimateRevenueOpportunities||"Not found in available data."}\n\nRecommended Leadership Action:\n${s.recommendedLeadershipAction||"Not found in available data."}`;}
function focusPriorities(summary){const items=[];if(summary.flaggedJobs)items.push("Validate flagged QC items in ServiceTitan before coaching or escalating.");if(!String(summary.requiredFormIssues||"").startsWith("No required form"))items.push("Coach required form completion: Customer Water Readings V2 and Plumbing Inspection V2.");if(!String(summary.fieldproSalesproUsageIssues||"").startsWith("No FieldPro"))items.push("Reinforce FieldPro/Sales Pro documentation.");if(!String(summary.paymentItemsNeedingReview||"").startsWith("No payment"))items.push("Review payment items before treating jobs as fully closed.");if(!String(summary.estimateRevenueOpportunities||"").startsWith("No estimate"))items.push("Review estimate/revenue opportunities and assign follow-through.");return items.slice(0,3);}
module.exports={analyzeQcData,formatDailySummary,focusPriorities};
